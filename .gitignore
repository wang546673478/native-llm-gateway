# Local config (may contain real API keys)
config.yaml
config.yaml.bak-*
config.yaml.bak.*

# Claude Code 本地工作区(worktree checkout,不入库)
.claude/worktrees/

# Build artifacts
backend/bin/
bin/
*.exe
*.test
*.out
frontend/tsconfig.tsbuildinfo

# 根目录编译产物(历史遗留曾被跟踪,现已取消)
/llm-gateway

# 根目录一次性排障脚本(package main,会跟 backend 模块冲突;查完即删)
/check_*.go
/list_*.go
/exec_sql.go
/fix_*.go
/migrate_and_fix.go
/create_relay_station.sql

# Local data
data/
*.db
*.db-journal
key-state.json
key-state.json.backup-*

# IDE
.idea/
.vscode/
*.swp
logs/
